# 智能测试数据生成工具 — 云服务器部署手册（原生部署版）

> 适用版本：test-data-generator 1.0.0
> 部署方式：原生部署（JDK 11 运行 Spring Boot jar + Nginx 反向代理，不依赖 Docker；数据库使用独立的云数据库 RDS）

---

## 1. 部署架构

```
用户浏览器 --(80/443)--> Nginx --(反向代理 127.0.0.1:8080)--> Java 应用 (JDK 11) --(JDBC 内网)--> 云数据库 MySQL 8.0 (RDS)
```

Nginx 与 Java 应用部署在 ECS 上，数据库使用独立的云数据库（RDS），通过内网连接：

| 组件 | 版本 | 地址 / 端口 | 说明 |
|------|------|---------|------|
| Nginx | 1.18+ | 本机 80 / 443 | 反向代理、静态资源缓存 |
| Java 应用 | JDK 11 + Spring Boot jar | 本机 8080 | 业务应用，prod 配置 |
| 云数据库 MySQL | 8.0 | RDS 内网地址 : 3306 | 托管实例，业务库 testdatagen（表由应用自动创建） |

## 2. 服务器要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2 核 | 4 核 |
| 内存 | 4 GB | 8 GB |
| 磁盘 | 40 GB | 60 GB+（数据量大时） |
| 系统 | CentOS 7+ / Ubuntu 20.04+ / Alibaba Cloud Linux | 同左 |
| 网络 | 公网出口（需调用 LLM API） | 同左 |

**安全组需开放端口：**

| 端口 | 用途 | 建议授权范围 |
|------|------|------------|
| 80 | HTTP 访问 | 0.0.0.0/0 |
| 443 | HTTPS 访问 | 0.0.0.0/0 |
| 22 | SSH 管理 | 办公网段 |

> 数据库使用云数据库并通过内网连接，ECS 安全组**无需开放 3306**；但需将 ECS 的内网 IP 加入 RDS 实例的白名单（见第 4.3 节）。

## 3. 安装 JDK 11

### 3.1 CentOS / Alibaba Cloud Linux

```bash
sudo yum install -y java-11-openjdk java-11-openjdk-devel
```

### 3.2 Ubuntu / Debian

```bash
sudo apt-get update
sudo apt-get install -y openjdk-11-jdk
```

### 3.3 验证

```bash
java -version
# 期望输出: openjdk version "11.x.x"
```

## 4. 准备云数据库（RDS）

数据库使用云厂商托管的 MySQL 实例（如阿里云 RDS / 腾讯云云数据库等），ECS 上无需安装 MySQL。

### 4.1 创建实例

在云厂商控制台创建实例，关键要求：

- 版本 MySQL 8.0，字符集 utf8mb4；
- **与 ECS 同地域、同 VPC**（保证内网互通）；
- 规格建议 2 核 4GB 起（测试环境可降配）。

### 4.2 创建数据库与账号

通过控制台「账号管理 / 数据库管理」页面创建，也可用 SQL 创建，等价 SQL 如下（将 `<强密码>` 替换为实际密码）：

```sql
CREATE DATABASE testdatagen CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE USER 'testdatagen'@'%' IDENTIFIED BY '<强密码>';
GRANT ALL PRIVILEGES ON testdatagen.* TO 'testdatagen'@'%';
FLUSH PRIVILEGES;
```

> 应用表由 JPA 在首次启动时通过 `ddl-auto=update` 自动创建维护，无需手工建表或导入 SQL 脚本。

### 4.3 配置白名单并记录连接地址

将 **ECS 的内网 IP** 加入 RDS 实例白名单（不建议开通公网访问）。配置完成后，在控制台记录实例的**内网连接地址**与端口（通常 3306），后续应用配置会用到。

### 4.4 验证连接

在 ECS 上安装 MySQL 客户端（仅客户端，不装服务端）：

```bash
sudo yum install -y mysql               # CentOS
sudo apt-get install -y mysql-client    # Ubuntu
```

测试连接云数据库：

```bash
mysql -h <RDS内网地址> -P 3306 -utestdatagen -p -e "SELECT 1"
```

连接成功并返回 1 即配置完成。

## 5. 准备应用包

应用为单个可执行 jar：`test-data-generator-1.0.0.jar`。

**方式一：本地构建后上传（推荐）**

在本地开发机执行（需 JDK 11）：

```bash
./mvnw clean package -DskipTests
# 产物：target/test-data-generator-1.0.0.jar
```

上传到服务器：

```bash
scp target/test-data-generator-1.0.0.jar root@<ECS-IP>:/opt/testdatagen/app.jar
```

**方式二：服务器上直接构建**

```bash
sudo yum install -y git maven          # CentOS；Ubuntu 用 apt-get
cd /opt
git clone <你的仓库地址> testdatagen-src
cd testdatagen-src
mvn clean package -DskipTests
cp target/test-data-generator-1.0.0.jar /opt/testdatagen/app.jar
```

创建运行目录：

```bash
sudo mkdir -p /opt/testdatagen
```

## 6. 配置环境变量

创建 `/opt/testdatagen/.env`（systemd 将以此文件注入环境变量）：

```bash
sudo vi /opt/testdatagen/.env
```

内容如下（替换为真实值）：

```bash
# --- 云数据库（RDS）---
MYSQL_HOST=<RDS内网地址>
MYSQL_PORT=3306
MYSQL_DATABASE=testdatagen
MYSQL_USER=testdatagen
MYSQL_PASSWORD=<应用账号密码>

# --- LLM API（至少配置一组，用于智能字段生成）---
OPENAI_API_KEY=<你的Key>
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-v4-pro

DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=<你的Key>

MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/
MIMO_API_KEY=<你的Key>              # 不使用可留空

# --- 知识库（可选，默认关闭）---
KB_ENABLED=false

# --- 安全（必须修改）---
# 数据库密码加密密钥，32 位十六进制，可用命令生成：
# openssl rand -hex 16
ENCRYPTION_KEY=<32位十六进制串>

# --- JVM 参数（按服务器内存调整）---
JAVA_OPTS=-Xms256m -Xmx512m
```

> **【安全提醒】**：
> 1. `.env` 包含密钥，请设置权限 `sudo chmod 600 /opt/testdatagen/.env`，切勿提交到 Git 仓库或对外暴露；
> 2. `ENCRYPTION_KEY` 一经使用不要更换，否则已加密存储的数据库连接密码无法解密。

## 7. 配置 systemd 服务并启动应用

创建服务文件 `/etc/systemd/system/testdatagen.service`：

```bash
sudo vi /etc/systemd/system/testdatagen.service
```

内容：

```ini
[Unit]
Description=Test Data Generator
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/testdatagen
EnvironmentFile=/opt/testdatagen/.env
ExecStart=/usr/bin/java $JAVA_OPTS -jar /opt/testdatagen/app.jar --spring.profiles.active=prod
SuccessExitStatus=143
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动并设置开机自启：

```bash
sudo systemctl daemon-reload
sudo systemctl enable testdatagen
sudo systemctl start testdatagen
```

验证：

```bash
sudo systemctl status testdatagen     # 应为 active (running)
curl http://127.0.0.1:8080/actuator/health
# 期望输出: {"status":"UP"}
```

> 首次启动时，应用通过 JPA `ddl-auto=update` 自动在 `testdatagen` 库建表，并初始化管理员账号（详见登录页提示）。

## 8. 安装并配置 Nginx

### 8.1 安装

```bash
sudo yum install -y nginx                    # CentOS
sudo apt-get install -y nginx                # Ubuntu
sudo systemctl enable nginx && sudo systemctl start nginx
```

### 8.2 配置反向代理

新建 `/etc/nginx/conf.d/testdatagen.conf`：

```bash
sudo vi /etc/nginx/conf.d/testdatagen.conf
```

内容：

```nginx
upstream app_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}

server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://app_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
        proxy_pass http://app_backend;
        proxy_set_header Host $host;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 禁止访问敏感路径
    location ~ /h2-console {
        deny all;
    }
}
```

校验并重载：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 8.3 验证部署

```bash
curl -I http://127.0.0.1/
# 期望输出: HTTP/1.1 200 OK
```

浏览器访问：`http://<服务器公网IP>`，看到登录页即部署成功。

## 9. HTTPS 配置（可选）

1. 将证书文件上传至 `/etc/nginx/ssl/`：
   - `fullchain.pem`（证书链）
   - `privkey.pem`（私钥）
2. 修改 `/etc/nginx/conf.d/testdatagen.conf`：
   - 在 80 端口 server 块中增加 `return 301 https://$host$request_uri;`；
   - 新增 443 server 块，指向证书路径，`server_name` 改为你的域名：

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://app_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 120s;
    }
}
```

3. 校验并重载：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 10. 日常运维

### 10.1 常用命令

```bash
sudo systemctl status testdatagen      # 查看应用状态
sudo systemctl restart testdatagen     # 重启应用
sudo systemctl stop testdatagen        # 停止应用
journalctl -u testdatagen -f           # 查看应用日志
mysql -h <RDS内网地址> -utestdatagen -p -e "SELECT 1"   # 测试数据库连接
sudo nginx -t && sudo systemctl reload nginx   # 校验并重载 Nginx
```

### 10.2 版本更新

```bash
# 上传新 jar 覆盖旧包
scp test-data-generator-1.0.0.jar root@<ECS-IP>:/opt/testdatagen/app.jar

# 重启应用生效
sudo systemctl restart testdatagen
journalctl -u testdatagen -f           # 观察启动日志确认无异常
```

### 10.3 数据备份

```bash
# 手动备份（在 ECS 上对云数据库执行）
mysqldump -h <RDS内网地址> -P 3306 -utestdatagen -p testdatagen > backup_$(date +%Y%m%d).sql

# 恢复（先建库：CREATE DATABASE testdatagen CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;）
mysql -h <RDS内网地址> -P 3306 -utestdatagen -p testdatagen < backup_20260827.sql
```

定时备份（每天凌晨 2 点）：

```bash
crontab -e
0 2 * * * mysqldump -h <RDS内网地址> -utestdatagen -p'<密码>' testdatagen > /opt/db_backup/backup_$(date +\%Y\%m\%d).sql
```

> 云数据库自带自动备份策略，建议在控制台同时开启自动备份并设置保留天数；上面的手动/定时备份可作为补充。

## 11. 常见问题排查

### Q1：应用启动失败 / 反复重启

```bash
journalctl -u testdatagen -n 100
```

常见原因：
- **云数据库连接失败**：检查 `.env` 中 `MYSQL_HOST` 是否为内网地址、ECS 内网 IP 是否已加入 RDS 白名单、账号密码是否正确；先用 `mysql -h <RDS内网地址> -utestdatagen -p` 验证连通性；
- **`.env` 格式问题**：systemd 的 EnvironmentFile 要求每行为 `KEY=VALUE`，不要有 `export` 前缀；修改后需 `sudo systemctl daemon-reload && sudo systemctl restart testdatagen`。

### Q2：80 端口无法访问

1. 确认云厂商安全组已放行 80 端口；
2. 确认系统防火墙：`sudo firewall-cmd --list-ports`（CentOS）或 `sudo ufw status`（Ubuntu）；
3. `ss -tlnp | grep 8080` 确认应用监听正常，`sudo nginx -t` 确认配置无误。

### Q3：LLM 相关功能报错

- 确认 `.env` 中至少一组 API Key 有效，且服务器可访问外网：
  `curl -I https://api.deepseek.com`；
- 修改 Key 后需重启应用：`sudo systemctl restart testdatagen`。

### Q4：内存不足（应用被 OOM Kill）

调小 `.env` 中的 `JAVA_OPTS`（如 `-Xms256m -Xmx384m`）并重启应用；或在云控制台升级数据库实例规格；必要时升级服务器配置。

### Q5：中文乱码

确认数据库字符集为 `utf8mb4`（创建 RDS 实例与数据库时应已选择）：

```sql
SHOW VARIABLES LIKE 'character_set%';
SHOW CREATE DATABASE testdatagen;
```

若有异常，在云控制台的参数设置中修改 `character_set_server` 为 `utf8mb4`，并以正确字符集重建受影响的数据库。

---

## 附录：部署检查清单

- [ ] 服务器满足配置要求，安全组放行 80/443（无需放行 3306）
- [ ] 云数据库 RDS 实例创建完成（MySQL 8.0、与 ECS 同 VPC），白名单已加入 ECS 内网 IP
- [ ] 云数据库账号与 `testdatagen` 库创建完成，连接验证通过
- [ ] JDK 11 安装完成（`java -version` 正常）
- [ ] `/opt/testdatagen/app.jar` 与 `.env` 就位，`MYSQL_HOST` 为 RDS 内网地址，`.env` 权限 600
- [ ] `testdatagen` systemd 服务已启动，健康检查返回 UP
- [ ] Nginx 配置完成，浏览器访问 `http://<公网IP>` 可看到登录页
- [ ] （可选）HTTPS 配置完成
- [ ] （可选）数据库定时备份已配置
