# 内网代码库重建与迁移指南

> 适用项目：智能测试数据生成工具（test-data-generator 1.0.0）
> 目标：将代码库迁移至公司内网，并推送到内网 Gitee

---

## 1. 迁移包内容说明

本地仓库已完成清理，git 工作区中仅保留项目必需内容：

```
src/                    全部业务源码（Java + 前端静态资源）与测试代码
deploy/                 部署配置（MySQL 初始化脚本、Nginx 配置）
.mvn/wrapper/           Maven Wrapper 配置
pom.xml                 Maven 工程定义
application*.yml        应用配置（位于 src/main/resources）
.env.example            环境变量模板（密钥已替换为占位符）
Dockerfile              Docker 构建文件（内网如不用 Docker 可忽略）
docker-compose.yml      容器编排文件（同上）
deploy.sh               一键部署脚本
README.md               项目说明
部署手册.md              云服务器部署手册（原生部署 + 云数据库方案）
内网代码库重建与迁移指南.md  本文档
```

**已清理的内容**（不再随迁移包携带）：
- 全部截图、展示/汇报材料（设计文档、演示文档、评估报告等）
- `.mvn/maven` 内置 Maven 发行版（内网请使用系统 Maven 构建）
- 构建产物（target/）、H2 运行数据（data/）、IDE 与工具缓存
- `.env.example` 中的真实 API 密钥已替换为占位符

## 2. 迁移步骤

### 2.1 在内网 Gitee 创建空仓库

1. 登录公司内网 Gitee，进入个人/团队空间；
2. 新建仓库，名称建议 `test-data-generator`；
3. **不要勾选**"使用 README 初始化仓库"（保持空仓库，避免推送冲突）；
4. 记录仓库地址，例如：
   - HTTPS：`http://gitee.公司内网域名/团队/test-data-generator.git`
   - SSH：`git@gitee.公司内网域名:团队/test-data-generator.git`

### 2.2 将代码带入内网机器

将整个项目目录（**必须包含 `.git` 目录**）通过公司允许的介质（光盘、内网文件传输、U 盘等）拷贝到内网开发机，例如放到 `D:\workspace\test1`。

> `.git` 目录携带了完整提交历史。拷贝完成后可先验证：
> `git log --oneline -3` 能看到提交记录即正常。

### 2.3 修改远程仓库并推送

在项目目录执行：

```bash
# 查看当前远程（原为 GitHub）
git remote -v

# 方式一：新增内网远程（推荐，保留历史远程信息）
git remote add gitee <内网Gitee仓库地址>
git push -u gitee master

# 方式二：直接替换原远程
git remote set-url origin <内网Gitee仓库地址>
git push -u origin master
```

如默认分支为 `main`，将上述 `master` 换成 `main`（用 `git branch` 查看）。

**SSH 推送需先配置密钥**（HTTPS 方式输入账号密码或私人令牌即可）：

```bash
ssh-keygen -t ed25519 -C "你的邮箱"
# 将 ~/.ssh/id_ed25519.pub 内容添加到内网 Gitee 的 SSH 公钥设置中
ssh -T git@gitee.公司内网域名   # 验证连通
```

### 2.4 推送后验证

- 内网 Gitee 仓库页面能看到完整代码与提交历史；
- 在另一台内网机器执行 `git clone <仓库地址>` 验证可克隆。

## 3. 内网开发环境搭建

### 3.1 安装 JDK 11

- Windows：安装 OpenJDK 11 / Oracle JDK 11，配置 `JAVA_HOME` 与 `PATH`；
- Linux：`yum install -y java-11-openjdk-devel` 或 `apt-get install -y openjdk-11-jdk`。

验证：`java -version` 输出 11。

### 3.2 安装 Maven 并配置内网镜像

**内网无法访问 Maven 中央仓库**，必须使用公司私服镜像。安装 Maven 3.6+ 后，编辑 `conf/settings.xml`（或 `~/.m2/settings.xml`）：

```xml
<mirrors>
  <mirror>
    <id>company-nexus</id>
    <name>公司内网 Maven 私服</name>
    <url>http://nexus.公司内网域名/repository/maven-public/</url>
    <mirrorOf>*</mirrorOf>
  </mirror>
</mirrors>
```

> 私服地址请向公司基础设施团队索取。若私服中没有本项目依赖（Spring Boot 2.x、Druid、Fastjson 等），需请管理员从外网同步或自行上传。

首次构建建议先验证依赖能否全部拉取：

```bash
mvn dependency:go-offline
```

### 3.3 构建项目

```bash
mvn clean package -DskipTests
```

构建产物：`target/test-data-generator-1.0.0.jar`。

## 4. 内网运行环境配置

### 4.1 数据库

应用需要 MySQL 8.0（业务库 `testdatagen`，表由 JPA 自动创建）：

- 使用公司 RDS：建库建账号、加白名单，步骤见《部署手册》第 4 章；
- 使用内网自建 MySQL：建库 `testdatagen`（utf8mb4）并创建应用账号即可。

### 4.2 LLM 服务（重点注意）

应用的智能字段生成依赖大模型 API。内网环境通常**无法直连外网**的 DeepSeek 等公网服务，需按公司实际情况处理：

| 场景 | 处理方式 |
|------|---------|
| 公司有大模型网关/私有模型服务 | 将 `.env` 中 `OPENAI_BASE_URL`、`DEEPSEEK_BASE_URL` 等指向内网网关地址，Key 换成公司分配的 |
| 内网完全无 LLM 服务 | 应用仍可启动，规则匹配、SQL 解析、枚举/正则/序列等本地生成器正常可用；仅"LLM 智能生成"相关功能不可用 |

配置模板（复制 `.env.example` 为 `.env` 后填写）：

```bash
MYSQL_HOST=<数据库地址>
MYSQL_PORT=3306
MYSQL_DATABASE=testdatagen
MYSQL_USER=testdatagen
MYSQL_PASSWORD=<密码>

OPENAI_API_KEY=<内网分配的Key>
OPENAI_BASE_URL=<内网模型网关地址>
OPENAI_MODEL=<模型名>

ENCRYPTION_KEY=<openssl rand -hex 16 生成>
```

### 4.3 启动与验证

```bash
java -jar target/test-data-generator-1.0.0.jar --spring.profiles.active=prod
```

验证：

```bash
curl http://localhost:8080/actuator/health
# 期望输出: {"status":"UP"}
```

浏览器访问 `http://localhost:8080` 看到登录页即成功。完整部署（systemd + Nginx）参见《部署手册》。

## 5. 常见问题

### Q1：推送时报 `fatal: remote origin already exists`

说明已存在同名远程，用 `git remote set-url origin <新地址>` 替换，或改用其他远程名（如 `gitee`）。

### Q2：构建时依赖下载失败

确认 `settings.xml` 镜像配置正确且私服可达：`curl -I http://nexus.公司内网域名/`。个别依赖私服缺失时，请管理员从公网代理同步。

### Q3：应用启动报数据库连接失败

检查 `.env` 中数据库地址/端口/账号；确认内网防火墙或数据库白名单已放行应用所在机器。

### Q4：中文文件名在 git 中显示为八进制转义

执行一次即可恢复正常显示：

```bash
git config --global core.quotepath false
```

---

## 附录：迁移检查清单

- [ ] 内网 Gitee 空仓库已创建，地址已记录
- [ ] 项目目录（含 `.git`）已拷贝到内网机器
- [ ] `git log` 可见提交历史
- [ ] 远程已指向内网 Gitee 并推送成功
- [ ] 另一台机器可正常克隆
- [ ] JDK 11 + Maven 安装完成，`settings.xml` 已配内网镜像
- [ ] `mvn clean package -DskipTests` 构建成功
- [ ] 数据库准备完成，`.env` 配置完成
- [ ] 应用启动成功，健康检查返回 UP
- [ ] （如涉及）内网 LLM 网关地址已配置并验证
