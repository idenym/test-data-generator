# AI 能力测评模板

## 1. 测评概述

### 1.1 测评目标

对测试数据生成工具中的 AI（大语言模型）能力进行系统化评估，量化各模型在数据生成场景下的表现，为模型选型和 prompt 优化提供数据支撑。

### 1.2 测评范围

| 场景 | 对应方法 | 调用频率 | 说明 |
|------|---------|---------|------|
| A - 字段值生成 | `LlmService.generateBatchValues()` | 高频（每字段每批次） | 根据字段元数据生成 realistic 测试数据 |
| B - 规则建议 | `LlmService.suggestRules()` | 低频（每表一次） | 分析表结构，为每个字段推荐最佳生成规则 |

### 1.3 适用模型

在 `application.yml` 中配置的所有模型均需参与测评，建议至少覆盖：

- DeepSeek 系列（deepseek-chat / deepseek-reasoner）
- Mimo 系列（mimo-*）
- 通用模型（gpt-4o-mini / qwen 等）

---

## 2. 测评环境配置

### 2.1 模型参数

```yaml
openai:
  temperature: 0.8          # 测评时固定，确保可比性
  max-tokens: 4096
  enable-thinking: false    # 基础测评关闭；思维链测评单独开启
```

### 2.2 测试数据库

准备一个包含以下典型表结构的 MySQL 数据库：

```sql
-- 用户表（基础类型覆盖）
CREATE TABLE t_user (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(32) NOT NULL COMMENT '用户名',
    email VARCHAR(128) NOT NULL COMMENT '邮箱地址',
    age TINYINT UNSIGNED COMMENT '年龄',
    balance DECIMAL(12,2) DEFAULT 0.00 COMMENT '账户余额',
    status ENUM('ACTIVE','INACTIVE','BANNED') DEFAULT 'ACTIVE',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    bio TEXT COMMENT '个人简介'
);

-- 订单表（业务关联）
CREATE TABLE t_order (
    order_id VARCHAR(20) NOT NULL PRIMARY KEY COMMENT '订单号',
    user_id BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    amount DECIMAL(10,2) NOT NULL COMMENT '订单金额',
    order_status TINYINT DEFAULT 0 COMMENT '0-待付款 1-已付款 2-已发货 3-已完成 4-已取消',
    shipping_address VARCHAR(256) COMMENT '收货地址',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 商品表（边界测试）
CREATE TABLE t_product (
    sku VARCHAR(12) NOT NULL PRIMARY KEY COMMENT 'SKU编码(12位)',
    name VARCHAR(64) NOT NULL COMMENT '商品名称',
    price DECIMAL(8,2) NOT NULL COMMENT '价格',
    stock INT UNSIGNED DEFAULT 0 COMMENT '库存',
    weight DECIMAL(5,3) COMMENT '重量(kg)',
    tags JSON COMMENT '标签'
);
```

### 2.3 测评参数

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| 每组测试重复次数 | 5 | 消除随机性 |
| 批次大小（batchSize） | 10 | 与生产一致 |
| 每个用例生成总量 | 50 | 5次 x 10条 |

---

## 3. 场景 A：字段值生成测评

### 3.1 测试用例矩阵

#### 基础类型测试

| 用例ID | 表名 | 字段名 | 数据类型 | 约束条件 | 描述 | 重点检验 |
|--------|------|--------|---------|---------|------|---------|
| A-01 | t_user | username | VARCHAR(32) | NOT NULL | 用户名 | 长度 ≤ 32，非空，多样性 |
| A-02 | t_user | email | VARCHAR(128) | NOT NULL | 邮箱地址 | 格式合法性 (xxx@xxx.xxx) |
| A-03 | t_user | age | TINYINT UNSIGNED | NULLABLE | 年龄 | 范围 0-255，语义合理 (0-120) |
| A-04 | t_user | balance | DECIMAL(12,2) | DEFAULT 0 | 账户余额 | 精度正确，非负（语义） |
| A-05 | t_user | status | ENUM | DEFAULT 'ACTIVE' | 状态 | 仅输出枚举值之一 |
| A-06 | t_user | created_at | DATETIME | NOT NULL | 创建时间 | 日期格式合法 |
| A-07 | t_user | bio | TEXT | NULLABLE | 个人简介 | 语义相关，中文/多语言 |

#### 边界与特殊场景测试

| 用例ID | 表名 | 字段名 | 数据类型 | 特殊说明 | 重点检验 |
|--------|------|--------|---------|---------|---------|
| A-08 | t_product | sku | VARCHAR(12) | 短字符串，PK | 严格 ≤ 12 字符，不生成 UUID |
| A-09 | t_order | order_id | VARCHAR(20) | 订单号 | ≤ 20 字符，编码格式合理 |
| A-10 | t_order | amount | DECIMAL(10,2) | 订单金额 | 精度正确，正数，合理范围 |
| A-11 | t_product | weight | DECIMAL(5,3) | 可空 | 精度 3位小数，范围 0-99.999 |
| A-12 | t_order | order_status | TINYINT | 业务枚举 0-4 | 理解注释语义，生成 0-4 |
| A-13 | t_user | id | BIGINT UNSIGNED | AUTO_INCREMENT | AI 应跳过或生成合法正整数 |

#### 语义理解测试

| 用例ID | 描述输入 | 期望行为 |
|--------|---------|---------|
| A-14 | "中国大陆手机号" | 生成 11 位 1xx 开头号码 |
| A-15 | "IPv4 地址" | 格式 x.x.x.x，各段 0-255 |
| A-16 | "中国身份证号" | 18 位，校验位合理 |
| A-17 | "公司名称（中文）" | 中文企业名，带"有限公司"等后缀 |
| A-18 | "UUID" + VARCHAR(36) | 标准 UUID 格式（含连字符）|
| A-19 | "UUID" + VARCHAR(32) | 无连字符 UUID（prompt 有约束）|

### 3.2 评分维度与标准

#### D1 - 约束合规率（权重 40%）

检查每条生成值是否满足字段定义的硬约束：

| 检查项 | 判定规则 | 示例 |
|--------|---------|------|
| 类型正确 | 数值字段必须为有效数字 | "abc" → 不合规 |
| 长度合规 | VARCHAR(n) 字段值 ≤ n 字符 | VARCHAR(12) 生成 36 位 UUID → 不合规 |
| 非空合规 | NOT NULL 字段不得生成 null/"" | null → 不合规 |
| 范围合规 | UNSIGNED 不得为负；TINYINT ≤ 255 | -1 → 不合规 |
| 精度合规 | DECIMAL(m,n) 小数位 ≤ n | DECIMAL(10,2) 生成 3 位小数 → 不合规 |
| 枚举合规 | ENUM 字段值必须在枚举列表内 | 输出 "UNKNOWN" → 不合规 |

**计算公式：**
```
约束合规率 = 合规值数量 / 总生成值数量 × 100%
```

**评分标准：**
| 等级 | 合规率 | 得分 |
|------|--------|------|
| 优秀 | ≥ 98% | 36-40 |
| 良好 | 90%-97% | 28-35 |
| 合格 | 80%-89% | 20-27 |
| 不合格 | < 80% | 0-19 |

#### D2 - 语义相关度（权重 25%）

评估生成值与字段 description/comment 的语义匹配程度：

| 等级 | 描述 | 得分 |
|------|------|------|
| 高度相关 | 值完全符合描述语义，如"邮箱"→ 有效邮箱格式 | 23-25 |
| 基本相关 | 值大致符合语义但有细节偏差 | 15-22 |
| 弱相关 | 值类型正确但语义不匹配，如"年龄"→ 200 | 8-14 |
| 无关 | 完全无法对应描述语义 | 0-7 |

#### D3 - 多样性（权重 20%）

衡量生成值的分散程度，避免大量重复：

**计算公式：**
```
多样性得分 = 去重值数量 / 总生成值数量 × 100%
```

**评分标准：**
| 等级 | 多样性 | 得分 |
|------|--------|------|
| 优秀 | ≥ 90% 不重复 | 18-20 |
| 良好 | 70%-89% | 14-17 |
| 合格 | 50%-69% | 10-13 |
| 不合格 | < 50% | 0-9 |

**特殊说明：** ENUM 类型字段多样性以覆盖的枚举值比例计算。

#### D4 - 格式正确率（权重 15%）

检查 LLM 输出是否为合法 JSON 数组：

| 检查项 | 说明 |
|--------|------|
| JSON 可解析 | 响应能被 `JSON.parseArray()` 解析 |
| 数量匹配 | 数组长度 = 请求的 batchSize |
| 无多余内容 | 无 markdown 代码块、无额外解释文字 |

**计算公式：**
```
格式正确率 = 格式合规批次数 / 总批次数 × 100%
```

**评分标准：**
| 等级 | 正确率 | 得分 |
|------|--------|------|
| 优秀 | 100% | 15 |
| 良好 | ≥ 90% | 12-14 |
| 合格 | ≥ 75% | 9-11 |
| 不合格 | < 75% | 0-8 |

### 3.3 记录表模板

| 用例ID | 模型 | 批次 | 合规数/总数 | 约束合规率 | 语义评分 | 多样性 | 格式OK | 备注 |
|--------|------|------|-----------|-----------|---------|--------|--------|------|
| A-01 | deepseek-chat | 1/5 | /10 | % | /25 | % | Y/N | |
| A-01 | deepseek-chat | 2/5 | /10 | % | /25 | % | Y/N | |
| ... | ... | ... | ... | ... | ... | ... | ... | |

---

## 4. 场景 B：规则建议测评

### 4.1 测试用例

| 用例ID | 输入表 | 字段数 | 复杂度 | 说明 |
|--------|--------|--------|--------|------|
| B-01 | t_user | 8 | 中等 | 多种数据类型，有 ENUM、TEXT |
| B-02 | t_order | 6 | 中等 | 有外键关联、业务状态码 |
| B-03 | t_product | 6 | 中等 | 有 JSON 类型、短 VARCHAR |
| B-04 | 自定义复杂表 | 15+ | 高 | 包含复合主键、多外键、各种数据类型 |

### 4.2 评分维度

#### D1 - 规则类型准确度（权重 40%）

检查推荐的 ruleType 是否适合该字段：

| 字段特征 | 推荐规则类型 | 说明 |
|---------|-------------|------|
| 有明确格式（邮箱、手机号） | REGEX | 正则适合格式固定的字段 |
| 数值范围明确 | RANGE | 年龄、价格等 |
| 有限离散值 | ENUM | 状态码、类型字段 |
| 语义复杂无固定格式 | LLM_DESCRIPTION | 姓名、地址、简介等 |
| AUTO_INCREMENT / PK | 不应推荐 | AI 应跳过自增字段 |

**计算公式：**
```
类型准确率 = 推荐类型合理的字段数 / 应推荐字段总数 × 100%
```

#### D2 - 配置参数合理性（权重 35%）

| 规则类型 | 检查项 |
|---------|--------|
| REGEX | pattern 语法正确且能匹配期望格式 |
| RANGE | min ≤ max，类型匹配（int/float/date），范围语义合理 |
| ENUM | values 不为空，覆盖主要取值，weights 之和为 1 |
| LLM_DESCRIPTION | description 非空且语义明确 |

**评分（每字段 0-5 分）：**
- 5分：参数完全合理，可直接使用
- 3分：参数基本可用，需微调
- 1分：参数存在明显错误
- 0分：配置缺失或完全不可用

#### D3 - 覆盖率（权重 25%）

```
覆盖率 = AI 推荐了规则的非自增字段数 / 非自增字段总数 × 100%
```

### 4.3 记录表模板

| 用例ID | 模型 | 字段名 | 推荐ruleType | 类型是否合理 | 参数评分(0-5) | 备注 |
|--------|------|--------|-------------|-------------|--------------|------|
| B-01 | deepseek-chat | username | LLM_DESCRIPTION | Y/N | /5 | |
| B-01 | deepseek-chat | email | REGEX | Y/N | /5 | |
| ... | ... | ... | ... | ... | ... | |

---

## 5. 多模型对比测评

### 5.1 性能指标

| 指标 | 说明 | 测量方式 |
|------|------|---------|
| 平均响应时间 | 单次 API 调用耗时 | 计时 callOpenAi() |
| P95 响应时间 | 95% 分位延迟 | 取多次调用排序后 95% 位 |
| 成功率 | 非空有效响应占比 | 成功次数 / 总调用次数 |
| Token 效率 | 有效输出 token / 总 token | 从 API 响应 usage 字段获取 |
| 首次重试率 | 需要重试的请求占比 | 日志中 retry 次数统计 |

### 5.2 横向对比模板

| 模型 | 约束合规率 | 语义相关度 | 多样性 | 格式正确率 | 平均延迟(ms) | 成功率 | 综合得分 |
|------|-----------|-----------|--------|-----------|-------------|--------|---------|
| deepseek-chat | % | /25 | % | % | ms | % | /100 |
| deepseek-reasoner | % | /25 | % | % | ms | % | /100 |
| mimo-* | % | /25 | % | % | ms | % | /100 |
| qwen-* | % | /25 | % | % | ms | % | /100 |

### 5.3 Extended Thinking 对比

针对支持 Extended Thinking 的模型（DeepSeek、Mimo），对比开启/关闭思维链时的表现差异：

| 模型 | Thinking | 约束合规率 | 语义评分 | 平均延迟 | Token消耗 | 收益评估 |
|------|---------|-----------|---------|---------|----------|---------|
| deepseek-reasoner | OFF | | | | | |
| deepseek-reasoner | ON | | | | | |
| mimo-* | OFF | | | | | |
| mimo-* | ON | | | | | |

---

## 6. 回退机制测评

### 6.1 测试场景

| 场景ID | 触发方式 | 期望行为 | 验证点 |
|--------|---------|---------|--------|
| F-01 | API Key 无效 | 3次重试后回退 DefaultGenerator | 数据仍可生成，日志有 WARN |
| F-02 | 模型返回空数组 "[]" | 回退 DefaultGenerator | 无异常，生成默认数据 |
| F-03 | 模型返回非 JSON | parseValuesFromResponse 解析失败 | 返回空列表，触发回退 |
| F-04 | 网络超时 | 指数退避重试 (1s, 2s, 4s) | 重试间隔正确 |
| F-05 | 模型返回数量不足 | 继续循环请求补充 | 最终数据量满足 totalRows |

### 6.2 记录表

| 场景ID | 注入方式 | 是否触发回退 | 回退后数据质量 | 日志是否正确 | 用户体验影响 |
|--------|---------|-------------|--------------|-------------|-------------|
| F-01 | 配置错误 Key | Y/N | 合格/不合格 | Y/N | 说明 |
| ... | ... | ... | ... | ... | ... |

---

## 7. 综合评分

### 7.1 场景 A 综合得分计算

```
场景A得分 = 约束合规率得分(40%) + 语义相关度得分(25%) + 多样性得分(20%) + 格式正确率得分(15%)
```

满分 100 分。

### 7.2 场景 B 综合得分计算

```
场景B得分 = 类型准确度(40%) + 参数合理性(35%) + 覆盖率(25%)
```

满分 100 分。

### 7.3 模型总评分

```
模型总分 = 场景A得分 × 0.7 + 场景B得分 × 0.3
```

（场景 A 权重更高，因为是核心高频功能）

### 7.4 评级标准

| 等级 | 总分范围 | 结论 |
|------|---------|------|
| S | ≥ 90 | 卓越，推荐作为默认模型 |
| A | 80-89 | 优秀，可作为主力模型 |
| B | 70-79 | 良好，可作为备选模型 |
| C | 60-69 | 合格，需关注短板 |
| D | < 60 | 不合格，不推荐使用 |

### 7.5 结论模板

```
## 测评结论

### 测评信息
- 测评日期：YYYY-MM-DD
- 测评人：
- 测试数据库：
- 测评版本：

### 模型排名
1. [模型名] - 总分 XX 分（等级 X）
2. [模型名] - 总分 XX 分（等级 X）
3. ...

### 最佳模型推荐
- 综合最优：[模型名]（理由）
- 性价比最优：[模型名]（理由）
- 延迟最低：[模型名]（理由）

### 发现的问题
1. [问题描述 + 影响范围 + 建议修复方式]
2. ...

### 优化建议
1. [Prompt 优化建议]
2. [参数调优建议]
3. [模型组合策略建议]
```

---

## 8. 执行清单

按以下顺序执行测评：

- [ ] 准备测评环境（数据库建表、模型配置）
- [ ] 执行场景 A 基础类型测试（A-01 ~ A-07）
- [ ] 执行场景 A 边界测试（A-08 ~ A-13）
- [ ] 执行场景 A 语义理解测试（A-14 ~ A-19）
- [ ] 执行场景 B 规则建议测试（B-01 ~ B-04）
- [ ] 执行多模型对比（每个模型重复上述测试）
- [ ] 执行 Extended Thinking 对比测试
- [ ] 执行回退机制测试（F-01 ~ F-05）
- [ ] 汇总数据，计算综合得分
- [ ] 填写结论模板，输出测评报告
