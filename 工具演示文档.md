# 智能测试数据生成工具 · 演示文档

---

## 目录

- 一、现状与痛点
- 二、方案设计与亮点
- 三、成果展示
- 四、流程演示

---

## 一、现状与痛点

### 1.1 现状

测试数据构造是软件测试流程中不可或缺的基础环节，直接影响测试的覆盖度与有效性。当前，测试团队在造数时普遍依赖以下方式：手工编写 INSERT 语句、编写脚本批量生成、调用 mock 工具产出随机数据、利用历史数据复制或脱敏、借助 AI 大模型直接生成等。这些方式各有局限，难以独立应对日益复杂的业务场景——尤其是涉及多表关联、码值对齐、复合约束的大数据构造需求。

### 1.2 痛点

**1. 大规模数据构造存在天花板**

当数据构造涉及的 SQL 逻辑过于复杂或关联表过多时，无论是人工编写还是 AI 直接生成，都容易因信息量过大超出处理能力上限，导致构造失败或数据质量不可控；复杂关联关系下，AI 对表间依赖的解析易出现误判，最终产出的数据无法通过业务校验。

**2. 多种造数方式相互孤立，无法融合**

手工造数擅长精确控制但效率极低，mock 工具产出快但真实感差，AI 生成语义丰富但约束合规率不稳定——每种方式各有适用场景，但测试人员的实际需求往往是混合型的：既需要多表关联一致性，又需要单字段语义真实感，还需要特定码值精确匹配，单一方式无法覆盖。

**3. 规则配置工作量大，且易与预期不符**

多表场景下要求用户为每张表的每个字段逐一配置造数规则，字段数随表数线性增长，配置工作量巨大；同时，用户对规则的理解可能与业务语义存在偏差，导致生成的数据"合规但不合理"，与实际预期不符。

---

## 二、方案设计与亮点

### 2.1 产品定位

**基于 AI 大模型的智能测试数据生成平台**：TestDataGen 是一个基于 AI 的智能测试数据生成工具，核心目标是解决传统测试数据生成工具存在的痛点，通过结合人工智能大模型能力和数据库元数据解析技术，自动生成符合业务规则和数据库约束的高质量测试数据。

### 2.2 整体工作流程

平台采用「五步向导」设计，用户从 SQL 粘贴到数据落库全程零编码：

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ ① 配置   │───▶│ ② 输入  │───▶│ ③ 配置  │───▶│ ④ 预览  │───▶│ ⑤ 写入  │
│   连接   │    │   SQL   │    │   规则  │    │   数据  │    │ 数据库  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘
 多环境连接      解析表结构        字段策略        实时调整        事务写入
 加密存储        关系分析        AI/正则/枚举     列级重生成       历史记录
```

### 2.3 核心设计思路

| 设计点 | 思路 |
|-------|------|
| **AI + 规则双引擎** | LLM 负责"语义合理"，规则引擎负责"约束合规"，两者互补而非取代 |
| **SQL 驱动一切** | 一条业务 SQL 即驱动表结构识别、关联关系发现、约束自动推导、生成顺序拓扑排序 |
| **五级规则匹配** | 关联字段 → 用户规则 → 存储规则 → 启发式规则 → 默认生成器，最大化复用历史经验 |
| **快照可追溯** | 每个任务保留规则快照、分析快照、数据快照，问题可复现可审计 |
| **降级保底** | 大模型异常 → 内置规则；JSON 解析失败 → 兜底；超长 → 截断；多模型轮询切换 |

### 2.4 亮点一：简单高效的用户操作

传统测试数据工具往往需要用户逐字段配置生成规则，多表场景下配置工作量随字段数线性增长。本平台从三个层面系统性降低了用户操作成本：

**层面一：SQL 约束自动推导 — "SQL 即规则"**

用户只需粘贴一条业务 SQL，平台即自动从 `WHERE` 和 `JOIN ON` 条件中提取语义约束，反向转化为字段生成规则，无需手工配置：

```sql
-- 用户输入的 SQL
WHERE business_status IN ('ACTIVE', 'INACTIVE')
  AND establish_date >= '2020-01-01'
  AND establish_date <= '2024-12-31'
  AND age > 18
```

平台自动推导：
- `business_status` → 枚举规则 `['ACTIVE', 'INACTIVE']`（IN 列表提取）
- `establish_date` → 范围规则 `[2020-01-01, 2024-12-31]`（同列 `>=` 与 `<=` 自动合并取交集）
- `age` → 范围规则 `min=18`（比较运算符提取）

**层面二：规则自动回填 + AI 一键推荐**

进入规则配置页时，系统自动执行多层回填，用户大多数情况下只需确认即可：

```
回填优先级（由高到低）：
  ① 已保存规则   — 上次任务中用户手动确认过的规则
  ② WHERE 推导   — 从 SQL 约束自动推导（标记为"WHERE"来源，黄色标识）
  ③ 历史规则     — 其他任务中相同字段的规则（标记为"HISTORY"，蓝色标识）
  ④ 知识库匹配   — 企业知识库中的规则建议（标记为"KNOWLEDGE_BASE"，紫色标识）
  ⑤ 启发式识别   — 内置规则库自动匹配（如 email 列 → 邮箱正则）
```

若自动回填仍不满意，用户可一键触发"AI 建议规则"，由大模型分析整张表结构后批量输出每列建议，直接回填到配置表单。

**层面三：预览即所得，一键写入**

配置完成后点击"预览"即可看到生成效果，不满意可列级重新生成或单元格双击编辑，确认后一键写入数据库。全表事务原子提交，任意表失败自动回滚。

### 2.5 亮点二：多元化造数规则支持

**四种核心策略**：

| 策略 | 精确度 | 典型用途 | 实现引擎 |
|------|--------|---------|---------|
| **AI 语义生成** | 语义级 | 姓名、地址、公司名、订单备注 | 大模型批量生成 |
| **正则表达式** | 格式级 | 身份证号、手机号、信用代码 | RgxGen 正则反向生成 |
| **范围值** | 数值级 | 金额、年龄、日期区间 | 区间均匀随机 |
| **枚举值** | 离散级 | 状态码、性别、业务类型 | 加权随机抽样 |

**五级规则匹配优先级**：

```
① 关联字段列（最高优先） → ForeignKeyGenerator，自动从父表 PK 值池引用
② 用户规则           → 本次任务中用户明确指定的规则
③ 存储规则           → 数据库中保存的规则，支持通配符模式
④ 启发式规则         → 内置规则库自动识别（email → 邮箱正则，phone → 手机号等）
⑤ 默认生成器（兜底） → 基于数据类型（DataFaker）生成基础值
```

对 SQL 脚本进行拆分解析，提取脚本中的关联字段、WHERE 条件等关键信息，通过代码实现关联字段的造数操作，确保关联字段造数成功率达到 100%。基于"人机结合"理念，对重要功能进行拆分，确保重要字段造数成功率达 100%；对于其他造数功能，则交由 AI 处理并设置兜底逻辑，保障测试数据生成成功。

**列级无损重新生成**：预览后发现某列数据不满意时，无需重新生成整张表。用户选中目标列，修改规则后仅该列重新调用生成器，其他列数据完全保持不变。关联字段列自动从已有父表数据中提取引用值，保证引用完整性。

### 2.6 亮点三：高效可控利用大模型

核心理念：**不是将库表结构和造数需求一股脑丢给大模型，而是通过 SQL 解析等预处理手段，将大模型调用收敛到"按字段维度"的精确调用，同时利用大模型的语义理解能力辅助规则推荐。**

平台采用"**预处理 + 按字段精确调用 + 规则引擎兜底**"的分层架构：

```
┌───────────────────────────────────────────────────────────────────────────┐
│  第一层：SQL 解析预处理（SqlParserService）                                │
│  · 解析表结构、字段类型、长度约束                                          │
│  · 提取 WHERE/ON 约束 → 自动推导为枚举/范围规则                           │
│  · 识别表间关联关系 → 自动绑定 ForeignKeyGenerator                        │
│  · 拓扑排序 → 确定多表生成顺序                                            │
├───────────────────────────────────────────────────────────────────────────┤
│  第二层：规则引擎匹配（RuleMatchingEngine）                                │
│  · 5 级优先级自动匹配：关联字段 → 用户规则 → 存储规则 → 启发式 → 默认生成器 │
│  · 正则/范围/枚举/序列等确定性策略由对应 Generator 处理                   │
│  · 仅 LLM_DESCRIPTION 类型的字段才进入大模型调用流程                      │
├───────────────────────────────────────────────────────────────────────────┤
│  第三层：按字段维度精确调用大模型（LlmService）                            │
│  · 每次调用只针对单个字段，Prompt 精确包含：                               │
│    表名、列名、数据类型、长度约束、可空性、语义描述                         │
│  · 单次请求返回 50 条值，存入线程安全值池；多个 LLM 字段并发调用           │
├───────────────────────────────────────────────────────────────────────────┤
│  第四层：容错降级（全链路兜底）                                            │
│  · API 调用失败 → 重试 3 次（指数退避）                                   │
│  · 全部重试失败 → DefaultGenerator 按数据类型兜底生成                     │
│  · 并发异常 → 回退串行模式；Markdown 响应 → 自动清理代码块标记             │
└───────────────────────────────────────────────────────────────────────────┘
```

**对比**：

| 维度 | 整表丢给 AI | 平台的"预处理+字段级调用" |
|------|------------|--------------------------|
| 单次 Prompt 信息量 | 全表 10+ 列结构 + 所有约束 | 单字段定义 + 精确约束 |
| Token 消耗 | 高（输入+输出均大规模） | 低（按字段拆分，每次仅单列） |
| 约束遵守率 | 随列数增长而下降 | 单列约束简单，遵守率极高 |
| 出错影响面 | 整表需重新生成 | 仅影响单列，可列级重生成 |
| 确定性策略 | 无法与 AI 混合编排 | 正则/范围/枚举/AI 自由组合 |
| FK 处理 | 模型难以保证引用完整性 | 引擎自动追踪 PK，FK 精确引用 |
| 容错能力 | 模型失败则整表无数据 | LLM 失败回退默认生成器 |

**AI 规则推荐**：除了数据生成，平台还利用大模型做"规则推荐"——让 AI 做它擅长的事（理解语义、推荐策略），而不是让它做确定性程序更擅长的事（格式校验、范围约束、FK 引用）。

**大模型幻觉问题的解决**：

大模型在生成测试数据时普遍存在"幻觉"问题——输出看似合理但实际不符合业务约束的数据（如编造不存在的行政区划代码、生成格式错误的证件号、产出与字段语义无关的随机文本）。平台通过两个层面系统性抑制幻觉：

**1. 知识库约束：用确定性知识替代模型猜测**

对于有明确业务规范的字段（手机号、身份证、统一社会信用代码、行政区划、银行卡号、业务状态码等），平台优先从知识库中匹配已验证的生成规则，**完全绕过大模型调用**，从根本上消除幻觉可能：

- 知识库中每个条目均经过验证，包含精确的正则 pattern、枚举值列表或范围约束
- 字段名/注释命中知识库时直接使用确定性生成器（REGEX/ENUM/RANGE），不走 LLM
- 命中知识库的字段响应延迟从 ~1.7s 降至 < 50ms，同时幻觉率降为 0

**2. 提示词按字段维度精准限制：缩小幻觉空间**

对于必须调用大模型的语义类字段（姓名、地址、公司名、备注等），平台通过将 Prompt 收敛到**单字段维度**，最大限度压缩模型的"自由发挥空间"：

- **约束显式化**：每次 Prompt 仅包含单个字段的完整约束信息（数据类型、最大长度、可空性、语义描述），模型需要"遵守"的规则极少，遵从率极高
- **输出格式固定**：要求模型严格返回 JSON 数组，格式简单明确，不会在格式上产生幻觉
- **长度硬约束**：Prompt 中明确标注 `maxLength`，即使模型偶尔超长，后置截断兜底确保数据不会因幻觉溢出
- **上下文隔离**：不同字段的 Prompt 互相独立，避免模型因上下文过长而"记混"约束、产生字段间串扰

对比：整表级 Prompt 中模型需同时满足 10+ 列约束，注意力分散导致幻觉频发；字段级 Prompt 中模型仅需关注 1 个字段的 1 组约束，幻觉空间被压缩到最小。

---

## 三、成果展示

### 3.1 效率价值

| 指标 | 传统方式 | 本工具 | 提升 |
|------|---------|-------|------|
| 单表 1000 行造数耗时 | ~30 分钟（手写 SQL） | < 30 秒 | **60x** |
| 7 表关联宽表造数耗时 | ~半天到一天 | < 2 分钟 | **60x~240x** |
| 字段规则配置时间 | 逐字段配置 | 60%+ 自动回填 | **3x~5x** |
| 调整单列数据 | 重新整表生成 | 列级重生 | **O(N) → O(1)** |

### 3.2 质量价值

- **关联关系 100% 命中**：宽表 LEFT JOIN 无 NULL miss
- **约束合规率 >= 95%**：长度、正则、枚举、唯一约束全部满足
- **AI 综合得分 92.0（S 级）**：deepseek-v4-pro 在字段值生成与规则建议两大场景实测
- **回退机制全通过**：网络异常、JSON 失败、长度超限、模型切换 4 项验证 100% 通过

### 3.3 AI 能力测评

对系统的两个核心 AI 能力（行级值生成、规则建议）建立了可复用的测评框架，并完成基线评测。

**模型对比与综合评分**

| 模型 | 场景 A（字段值生成） | 场景 B（规则建议） | **总分** | **等级** |
|------|--------|--------|--------|--------|
| deepseek-v4-pro | 94.6 | 85.9 | **92.0** | **S** |
| mimo-v2.5-pro | 82.5 | 75.4 | **80.3** | **A** |

**回退机制验证结果**

| 验证项 | 结果 |
|--------|------|
| 网络异常自动降级到规则生成 | 通过 |
| JSON 解析失败兜底 | 通过 |
| 超长数据截断兜底 | 通过 |
| 切换备用模型 | 通过 |

### 3.4 端到端验证（工商企业宽表）

专门构建了一个高复杂度的银行业"全量工商企业信息宽表"场景作为端到端验证基线：

**场景复杂度**：
- 7 张源表 + 1 张 80+ 字段宽表
- 9 个 LEFT JOIN + 5 个复合 ON 条件
- 5 类业务码值字典
- 3 个 UNIQUE 约束
- 多精度 DECIMAL（18,4 / 18,2 / 10,6 / 6,4）

**验证结果**：

| 设计功能 | 验证结果 |
|---------|---------|
| **多表依赖拓扑排序** | 严格按拓扑层级生成，无引用空值 |
| **关联关系严格满足** | 100% 关联命中，宽表关联无 NULL miss |
| **关联字段长度对齐** | 所有关联字段长度严格一致，无截断错位 |
| **复合 JOIN 条件遵从** | 子表生成时自动满足复合过滤条件 |
| **码值字典约束** | 5 类业务码值全部命中字典，无脏值 |
| **唯一约束遵从** | 批量插入无重复冲突 |
| **WHERE 约束转生成约束** | 生成数据 100% 满足 WHERE 条件 |
| **多精度 DECIMAL** | 各精度均按定义生成，无溢出 |

**结论**：场景中所有 80+ 字段、9 个 JOIN、5 类码值映射、3 个唯一约束、10+ 处 COALESCE 融合，**全部按设计预期跑通**，证明系统从 SQL 解析到数据落库的全链路在工业级复杂度下依然成立。

> 该场景已固化为标准回归基线，每次涉及 SQL 解析、依赖编排、约束推导的改动都会以此场景作为最低验收门槛。

---

## 四、流程演示

### Step 1 — 进入首页（执行历史）

- 登录工具，默认进入首页"执行历史"
- 列表展示所有已执行造数任务：状态、目标 SQL、关联表、行数、创建时间
- 支持刷新、筛选、查看详情
- 点击 **「新建任务」** 开始新一轮造数

![首页 - 执行历史列表](images/step_1_1.png)

### Step 2 — 数据库连接配置

- 选择数据库类型（MySQL / PostgreSQL / Oracle / ...）
- 填写连接参数：连接名称、IP、端口、账号、密码、数据库名
- 点击 **「测试连接」** → 提示"连接成功"
- 点击 **「保存」** 留存常用连接，下次直接选用
- 点击 **「下一步」** 进入 SQL 解析

![数据库连接配置](images/step_2_1.png)

### Step 3 — SQL 导入与解析

- 在编辑区输入业务 SQL（INSERT-SELECT 多表加工脚本）
- 支持本地 SQL 文件导入 / 常用脚本保存调用
- 点击 **「解析 SQL」**

![SQL 编辑器 - 输入业务加工SQL](images/step_2_2.png)

- 工具自动识别：
  - 目标表与字段（含类型、注释、长度、PK/AI/NN）
  - 表间关联关系
  - 生成拓扑顺序
  - WHERE/ON 约束

![解析结果 - 表关联关系 + 数据生成顺序](images/step_3_1.png)

- 解析结果以字段详情表展示：字段名、类型、属性、注释、关联引用一目了然

![解析结果 - 表结构字段详情](images/step_3_2.png)

### Step 4 — 字段规则配置

- 系统自动加载所有字段，**自动回填默认规则**（5 级优先级）
- 自动跳过：自增列、关联引用列（引擎自动处理）

![规则配置 - 商户表字段规则](images/step_4_1.png)

- 对剩余字段，选择规则类型并配置：
  - **AI 语义生成（LLM_DESCRIPTION）**：姓名、地址、公司名、订单备注等
  - **正则表达式（REGEX）**：身份证、手机号、统一社会信用代码等
  - **枚举值（ENUM）**：状态码、性别、省份、业务类型等
  - **范围值（RANGE）**：金额、年龄、数量、日期区间等

![规则配置 - 企业表字段规则](images/step_5_1.png)

![规则配置 - 企业表更多字段](images/step_5_2.png)

- 辅助功能：
  - **一键 AI 建议**：整表字段批量推荐
  - **字段历史规则**：查看/复用历史使用记录

### Step 5 — 数据生成

- 选择内置 AI 生成模型（deepseek-v4-pro / mimo-v2.5-pro）
- 填写本次生成行数
- 点击 **「生成数据」**
- 系统按规则并行批量构造：
  - 表间并行（同层无依赖表）
  - 字段级并发（每字段独立异步）
  - 50 条/批 LLM 批量调用

![数据生成 - 选择模型、设置行数、正在生成中](images/step_6_1.png)

### Step 6 — 数据预览与校验

- 预览表格展示完整数据
- 支持：
  - 单条数据手动修改
  - **单列数据重新生成**（列级无损刷新）
  - 字段格式 / 业务逻辑 / 数据范围核对
- 不满意 → 调整规则后重生该列，其他列保持不变

![数据预览 - 宽表数据预览](images/step_6_2.png)

### Step 7 — 数据入库与执行历史归档

- 校验通过 → 点击 **「写入数据库」**
- 系统事务化批量写入目标表
- 执行完成后自动返回执行结果：
  - 关联库表结构（ER 关系图）
  - 完整 SQL 语句
  - 造数规则快照
  - 数据写入结果 / 错误日志
- 任务自动归档至首页执行历史，保存三类快照（规则、分析、数据）
- 下次相同场景可一键基于历史任务复用

![执行结果 - ER关系图 + 任务快照详情](images/step_7_1.png)

---

## 演示地址

```
访问地址 http://8.130.81.216/
```

> 详细技术方案与架构细节参见 `PRODUCT_INTRO.md`；完整界面操作步骤参见 `智能测试数据生成工具操作手册.pdf`。
