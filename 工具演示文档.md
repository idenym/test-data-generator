# 智能测试数据生成工具 · 演示文档

> 一份面向产品宣讲、客户演示与团队对齐的综合材料，配合 `智能测试数据生成工具操作手册.pdf` 使用。

---

## 目录

- 一、方案设计
- 二、痛点
- 三、亮点
- 四、成果价值
- 五、流程演示
- 六、演示地址

---

## 一、方案设计

### 1.1 产品定位

**基于 AI 大模型的智能测试数据生成平台**：通过解析业务 SQL，自动理解数据库表结构与表间关联关系，将 AI 大模型的语义生成能力与规则引擎的精确约束能力深度融合，**一键生成既符合业务语义又满足所有数据库约束的真实感测试数据**，并以事务方式直接写入目标数据库，同时完整记录任务执行快照供追溯复现。

### 1.2 总体架构

```
┌────────────────────────────────────────────────────────┐
│             用户层（前端 SPA · Stepper 引导）            │
│   首页 → 连接 → SQL 解析 → 规则配置 → 生成 → 预览 → 入库 │
└────────────────────────────────────────────────────────┘
                          ↕  REST API
┌────────────────────────────────────────────────────────┐
│                  服务层（Spring Boot）                   │
│  SqlParserService · RuleMatchingEngine                 │
│  DataGenerationPipeline · LlmService                   │
│  KnowledgeBaseService · TaskHistoryService             │
└────────────────────────────────────────────────────────┘
                          ↕
┌────────────────────────────────────────────────────────┐
│                  能力层（双引擎融合）                     │
│  AI 大模型（语义理解）    +    规则引擎（精确约束）       │
│  · LLM_DESCRIPTION              · REGEX/ENUM/RANGE     │
│  · 字段语义生成                  · 长度/类型/枚举校验    │
└────────────────────────────────────────────────────────┘
                          ↕
┌────────────────────────────────────────────────────────┐
│  数据层：MySQL/PostgreSQL/Oracle… ｜ 任务快照库（三类）  │
└────────────────────────────────────────────────────────┘
```

### 1.3 核心设计思路

| 设计点 | 思路 |
|-------|------|
| **AI + 规则双引擎** | LLM 负责"语义合理"，规则引擎负责"约束合规"，两者互补而非取代 |
| **SQL 驱动一切** | 一条业务 SQL 即驱动表结构识别、关联关系发现、约束自动推导、生成顺序拓扑排序 |
| **五级规则匹配** | 关联字段 → 用户规则 → 存储规则 → 启发式规则 → 默认生成器，最大化复用历史经验 |
| **快照可追溯** | 每个任务保留规则快照、分析快照、数据快照，问题可复现可审计 |
| **降级保底** | 大模型异常 → 内置规则；JSON 解析失败 → 兜底；超长 → 截断；多模型轮询切换 |

### 1.4 关键技术

- **SQL 解析**：JSqlParser + 自研 ON/WHERE 约束抽取
- **AI 调用**：OpenAI 兼容协议，支持 deepseek-v4-pro / mimo-v2.5-pro 双模型，支持 Extended Thinking
- **生成引擎**：DataFaker + 自研正则反向求解 + LLM 批量调用
- **并发优化**：表间 `CompletableFuture.allOf` 并行 + 字段级专用线程池 + 50 条/批 LLM 调用
- **可视化**：纯 HTML5 Canvas 绘制 ER 关系图（无外部图形库依赖）

---

## 二、痛点

### 2.1 传统造数方式的核心痛点

| 痛点 | 具体表现 |
|------|---------|
| **效率低下** | 手写 INSERT、写脚本、调用 mock 工具，多表场景动辄半天起步 |
| **真实感差** | mock 工具产出的"张三-15812345678-test@mail.com"一眼假，业务测试无说服力 |
| **关联关系易错** | 多表 JOIN 场景下，子表外键值经常对不上父表，导致 LEFT JOIN 全是空 |
| **约束难满足** | 字段长度、正则、枚举、范围、唯一约束等需逐一手工保证，遗漏即批量失败 |
| **不可复现** | 哪天造的数？用什么规则？为什么这次和上次不一样？无任何记录 |
| **重复劳动** | 同一字段每个人都要重新踩坑，配过的优质规则没人沉淀，下次冷启动 |
| **跨表语义错位** | 即使单字段合规，行内字段间也常矛盾（如 birthday 与 age 不一致、name 与 gender 错位） |

### 2.2 复杂业务场景下的特定痛点

以银行业典型的"全量工商企业宽表"为例：

- **7 张源表 → 1 张 80+ 字段宽表**：手工造数几乎不可行
- **9 个 LEFT JOIN + 5 个复合 ON 条件**：稍有不慎就是 NULL miss
- **5 类业务码值字典**：手工对齐编码 ↔ 名称极易错
- **3 个 UNIQUE 约束**：批量插入冲突频发
- **多精度 DECIMAL（18,4 / 18,2 / 10,6 / 6,4）**：精度溢出问题屡见不鲜

### 2.3 现有 AI 造数工具的痛点

| 类型 | 局限 |
|------|------|
| 通用 LLM 直答 | 不懂表结构、不懂约束，给出的数据无法直接入库 |
| 纯规则工具 | 真实感差、无法理解字段语义 |
| 各方言脚本工具 | 学习成本高、无可视化、不支持复杂关联 |

---

## 三、亮点

### 3.1 SQL 一键解析，自动推导一切

输入一条业务 SQL，工具自动产出：

- **表清单与字段元数据**（类型、长度、注释、PK/AI/NN、关联关系）
- **表间关联关系图**（可视化 ER 图，可拖拽，按层着色）
- **数据生成拓扑顺序**（父表先于子表）
- **WHERE/ON 约束 → 生成规则**（如 `status IN ('1','2')` 自动转 ENUM）

### 3.2 智能规则回填（5 级优先级）

打开规则配置页，**60%+ 字段已自动配置完成**，用户仅需关注剩余字段：

| 优先级 | 来源 | 颜色标记 |
|-------|------|---------|
| 1 | WHERE/ON 条件自动推导 | 🟡 黄色 |
| 2 | 历史规则复用（同库同表同列） | 🔵 蓝色 |
| 3 | 知识库匹配（手机号/身份证等标准字段） | 🟣 紫色 |
| 4 | 字段注释语义识别 | ⚪ 灰色 |
| 5 | 启发式内置规则（邮箱、状态、性别等） | — |

### 3.3 一键 AI 建议规则

针对整张表，调用大模型基于（字段名+类型+注释+关联关系）一次性给出整表所有字段的最优规则建议，**实测 deepseek-v4-pro 综合评分 92.0（S 级）**。

### 3.4 关联关系强一致

- 关联字段值池由父表生成结果实时供应，**100% 命中**
- 关联字段长度严格对齐（VARCHAR(128) ↔ VARCHAR(128)，永不截断）
- 复合 ON 条件穿透到子表生成规则（如 `cert_status='1'` 自动应用到证件表生成）
- 同名同义字段长度对齐：父子表跨表关联字段长度自动校验对齐

### 3.5 列级无损重新生成

预览数据后某列不满意？

- 仅该列重新调用生成器
- 其他列数据完全保持不变
- 关联字段列自动从已有父表数据中提取引用值
- 调整效率从 O(全表) → O(单列)

### 3.6 完整快照可追溯

每次任务自动保存三类快照：

| 快照类型 | 内容 | 用途 |
|---------|------|------|
| **规则快照** | 本次使用的所有字段规则 | 复用 / 复盘 |
| **分析快照** | SQL 解析的表结构、关联、拓扑 | 排查问题 |
| **数据快照** | 实际写入的所有数据 | 回溯审计 |

### 3.7 多模型容错与降级

- 双模型轮询：deepseek-v4-pro（默认 S 级）+ mimo-v2.5-pro（备份 A 级）
- 网络异常 → 自动降级到内置规则生成
- JSON 解析失败 → 解析兜底
- 长度超限 → 截断兜底而非整批丢弃

### 3.8 可视化端到端引导

- Stepper 6 步引导，每步都有明确入口和校验
- 数据预览支持单条修改、单列重生
- ER 关系图可拖拽，长依赖链自动分层
- 任务执行历史一键查看

---

## 四、成果价值

### 4.1 效率价值

| 指标 | 传统方式 | 本工具 | 提升 |
|------|---------|-------|------|
| 单表 1000 行造数耗时 | ~30 分钟（手写 SQL） | < 30 秒 | **60×** |
| 7 表关联宽表造数耗时 | ~半天到一天 | < 2 分钟 | **60×~240×** |
| 字段规则配置时间 | 逐字段配置 | 60%+ 自动回填 | **3×~5×** |
| 调整单列数据 | 重新整表生成 | 列级重生 | **O(N) → O(1)** |

### 4.2 质量价值

- **关联关系 100% 命中**：宽表 LEFT JOIN 无 NULL miss
- **约束合规率 ≥ 95%**：长度、正则、枚举、唯一约束全部满足
- **AI 综合得分 92.0（S 级）**：deepseek-v4-pro 在字段值生成与规则建议两大场景实测
- **回退机制全通过**：网络异常、JSON 失败、长度超限、模型切换 4 项验证 100% 通过

### 4.3 业务价值

| 角色 | 收益 |
|------|------|
| **测试工程师** | 从"造数搬砖工"转向"用例设计师"，把时间花在测试逻辑而非造数本身 |
| **业务分析师** | 高真实感数据让业务测试场景具备说服力，需求评审更顺畅 |
| **DBA / 架构师** | SQL 驱动的解析 + 完整快照，数据来源可审计可追溯 |
| **管理层** | 测试数据准备从瓶颈环节变成可量化、可复用的产线能力 |

### 4.4 端到端验证（工商企业宽表）

在一个银行业真实复杂度的"全量工商企业信息宽表"场景中：

- 7 张源表 + 1 张 80+ 字段宽表 全部跑通
- 9 个 LEFT JOIN 全部命中，**无任何 NULL miss**
- 5 类码值字典严格遵从，**无脏值**
- 3 个 UNIQUE 约束批量插入**无冲突**
- 多精度 DECIMAL、TEXT 大字段、复合 ON 条件 **全部按设计跑通**

> 该场景已固化为标准回归基线，每次涉及 SQL 解析、依赖编排、约束推导的改动都以此为最低验收门槛。

---

## 五、流程演示

> 完整 7 步流程，每步附操作截图。

### Step 1 — 进入首页（执行历史）

- 登录工具，默认进入首页"执行历史"
- 列表展示所有已执行造数任务：状态、目标 SQL、关联表、行数、创建时间
- 支持刷新、筛选、查看详情
- 点击 **「新建任务」** 开始新一轮造数

![首页 - 执行历史列表](images/step_1_1.png)

> 💡 演示要点：强调"以历史为中心"的导航理念，老任务可一键复用规则。

### Step 2 — 数据库连接配置

- 选择数据库类型（MySQL / PostgreSQL / Oracle / …）
- 填写连接参数：连接名称、IP、端口、账号、密码、数据库名
- 点击 **「测试连接」** → 提示"连接成功"
- 点击 **「保存」** 留存常用连接，下次直接选用
- 点击 **「下一步」** 进入 SQL 解析

![数据库连接配置](images/step_2_1.png)

> 💡 演示要点：连接信息加密保存，支持复用，避免重复填写。

### Step 3 — SQL 导入与解析

- 在编辑区输入业务 SQL（INSERT-SELECT 多表加工脚本）
- 支持本地 SQL 文件导入 / 常用脚本保存调用
- 点击 **「解析 SQL」**

![SQL 编辑器 - 输入业务加工SQL（语法高亮）](images/step_2_2.png)

- 工具自动识别：
  - 目标表与字段（含类型、注释、长度、PK/AI/NN）
  - 表间关联关系
  - 生成拓扑顺序
  - WHERE/ON 约束

![解析结果 - 表关联关系 + 数据生成顺序](images/step_3_1.png)

- 解析结果以 **字段详情表** 展示：字段名、类型、属性、注释、关联引用一目了然

![解析结果 - 表结构字段详情（含关联引用标记）](images/step_3_2.png)

- 确认无误 → **「下一步：规则配置」**

> 💡 演示要点：拿一条 7 表 9 JOIN 的复杂 SQL 当场解析，2 秒内出图，最直观。

### Step 4 — 字段规则配置

- 系统自动加载所有字段，**自动回填默认规则**（5 级优先级）：
  - 🟡 WHERE 条件推导
  - 🔵 历史规则复用
  - 🟣 知识库匹配
  - ⚪ 字段注释识别
  - 启发式内置规则
- 自动跳过：自增列、关联引用列（引擎自动处理）

![规则配置 - 商户表字段规则（带颜色来源标记）](images/step_4_1.png)

- 对剩余字段，选择规则类型并配置：
  - **AI 语义生成（LLM_DESCRIPTION）**：姓名、地址、公司名、订单备注等
  - **正则表达式（REGEX）**：身份证、手机号、统一社会信用代码等
  - **枚举值（ENUM）**：状态码、性别、省份、业务类型等
  - **范围值（RANGE）**：金额、年龄、数量、日期区间等

![规则配置 - 企业表字段规则（AI生成+正则+枚举+范围混合）](images/step_5_1.png)

![规则配置 - 企业表更多字段（自动识别码值关联）](images/step_5_2.png)

- 辅助功能：
  - **一键 AI 建议**：整表字段批量推荐
  - **字段历史规则**：查看/复用历史使用记录
  - **空规则校验**：禁止带空配置进入下一步

> 💡 演示要点：演示"一键 AI 建议"对整表 80+ 字段批量生成规则，30 秒搞定原本要 1 小时的工作。

### Step 5 — 数据生成

- 选择内置 AI 生成模型（deepseek-v4-pro / mimo-v2.5-pro）
- 填写本次生成行数
- 再次核对目标表、SQL、关联表信息
- 点击 **「生成数据」**
- 系统按规则**并行批量构造**：
  - 表间并行（同层无依赖表）
  - 字段级并发（每字段独立异步）
  - 50 条/批 LLM 批量调用

![数据生成 - 选择模型、设置行数、正在生成中](images/step_6_1.png)

> 💡 演示要点：演示 7 表 1000 行 < 2 分钟跑完，进度实时刷新。

### Step 6 — 数据预览与校验

- 预览表格展示完整数据
- 支持：
  - 单条数据手动修改
  - **单列数据重新生成**（列级无损刷新）
  - 字段格式 / 业务逻辑 / 数据范围核对
  - 排查脏数据、异常数据
- 不满意 → 调整规则后重生该列，其他列保持不变

![数据预览 - 宽表数据预览（支持单列重新生成）](images/step_6_2.png)

> 💡 演示要点：演示"列级重生"——发现某列效果不好，30 秒重生该列，对比效果。

### Step 7 — 数据入库与执行历史归档

- 校验通过 → 点击 **「写入数据库」**
- 系统**事务化批量写入**目标表
- 等待执行完成 → 自动返回执行结果：
  - 关联库表结构
  - 完整 SQL 语句
  - 造数规则
  - 数据写入结果（成功）/ 错误日志（失败）
- 任务自动归档至首页执行历史，**保存三类快照**（规则、分析、数据）
- 下次相同场景可一键基于历史任务复用

![执行结果 - ER关系图 + 任务快照详情](images/step_7_1.png)

> 💡 演示要点：成功后立即回到首页，展示新任务在历史列表里的快照三件套，强调可复现可追溯。

## 六、演示地址
```
访问地址 http://8.130.81.216/
```

> 详细技术方案与架构细节参见 `PRODUCT_INTRO.md`；完整界面操作步骤参见 `智能测试数据生成工具操作手册.pdf`。
